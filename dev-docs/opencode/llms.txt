### Install dependencies and start development server

Source: https://github.com/anomalyco/opencode/blob/dev/CONTRIBUTING.md

Installs project dependencies and starts the dev server from the repository root using Bun 1.3+.

```bash
bun install
bun dev
```

--------------------------------

### Start development server with npm run dev

Source: https://github.com/anomalyco/opencode/blob/dev/packages/console/app/README.md

Start the local development server after installing dependencies, with an optional flag to automatically open the application in a new browser tab.

```bash
npm run dev

# or start the server and open the app in a new browser tab
npm run dev -- --open
```

--------------------------------

### Set up virtual framebuffer for headless Linux environments

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/troubleshooting.mdx

Installs xvfb, starts a virtual display, and sets the DISPLAY environment variable to enable clipboard support in headless environments.

```bash
apt install -y xvfb
# and run:
Xvfb :99 -screen 0 1024x768x24 > /dev/null 2>&1 &
export DISPLAY=:99.0
```

--------------------------------

### Install OpenCode using the install script

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/index.mdx

Download and install OpenCode using the official automated install script via curl.

```bash
curl -fsSL https://opencode.ai/install | bash
```

--------------------------------

### Install OpenCode on Windows using Scoop

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/index.mdx

Install OpenCode on Windows using the Scoop command-line installer.

```bash
scoop install opencode
```

--------------------------------

### Install @opencode-ai/sdk via npm

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/da/sdk.mdx

Installer OpenCode SDK-pakken fra npm.

```bash
npm install @opencode-ai/sdk
```

--------------------------------

### Install OpenCode on Windows using NPM

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/index.mdx

Install OpenCode globally on Windows through npm.

```bash
npm install -g opencode-ai
```

--------------------------------

### Run custom setup and return extra data with init

Source: https://github.com/anomalyco/opencode/blob/dev/packages/opencode/test/AGENTS.md

Provide an `init` function to perform custom directory setup. The returned value is accessible via `tmp.extra`.

```typescript
await using tmp = await tmpdir<string>({
  init: async (dir) => {
    await Bun.write(path.join(dir, "file.txt"), "content")
    return "extra data"
  },
})
// Access extra data via tmp.extra
console.log(tmp.extra) // "extra data"
```

--------------------------------

### Install OpenCode on Windows using Mise

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/index.mdx

Install and manage OpenCode globally on Windows via Mise.

```bash
mise use -g github:anomalyco/opencode
```

--------------------------------

### Start OpenCode server for desktop app connection

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/windows-wsl.mdx

Starts the server in WSL listening on 0.0.0.0 at port 4096 to allow external desktop app connections.

```bash
opencode serve --hostname 0.0.0.0 --port 4096
```

--------------------------------

### Install a plugin using the opencode plug alias

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/cli.mdx

Install a plugin module using the shorthand alias plug command.

```bash
opencode plug <module>
```

--------------------------------

### Install dependencies with Bun

Source: https://github.com/anomalyco/opencode/blob/dev/packages/opencode/README.md

Installs project dependencies using the Bun CLI.

```bash
bun install
```

--------------------------------

### Start web interface with opencode web

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/cli.mdx

Start a headless OpenCode HTTP server and open a web browser to access the interface. Set OPENCODE_SERVER_PASSWORD to enable HTTP basic authentication.

```bash
opencode web
```

--------------------------------

### Install OpenCode Desktop app via Homebrew or Scoop

Source: https://github.com/anomalyco/opencode/blob/dev/README.md

Installs the OpenCode Desktop beta application using Homebrew on macOS or Scoop on Windows.

```bash
# macOS (Homebrew)
brew install --cask opencode-desktop
# Windows (Scoop)
scoop bucket add extras; scoop install extras/opencode-desktop
```

--------------------------------

### Install OpenCode using Homebrew

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/index.mdx

Install OpenCode on macOS and Linux using the official OpenCode tap for the most up-to-date releases.

```bash
brew install anomalyco/tap/opencode
```

--------------------------------

### Install OpenCode on Arch Linux

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/index.mdx

Install the stable version from the official repositories using pacman or the latest build from AUR using paru.

```bash
sudo pacman -S opencode           # Arch Linux (Stable)
paru -S opencode-bin              # Arch Linux (Latest from AUR)
```

--------------------------------

### Install @opencode-ai/http-recorder and dependencies with Bun

Source: https://github.com/anomalyco/opencode/blob/dev/packages/http-recorder/README.md

Installs the required Effect 4 beta and HttpRecorder development packages. Supports Node.js 22+ and Bun.

```sh
bun add effect@4.0.0-beta.74
bun add -d @opencode-ai/http-recorder@beta @effect/vitest vitest
```

--------------------------------

### Install opencode GitHub Action via CLI

Source: https://github.com/anomalyco/opencode/blob/dev/github/README.md

Run this command from your local terminal within your repository to install the GitHub app, configure the workflow file, and set up secrets.

```bash
opencode github install
```

--------------------------------

### Start Slack bot development server with bun dev

Source: https://github.com/anomalyco/opencode/blob/dev/packages/slack/README.md

Starts the Slack bot service after configuring Slack credentials in `.env`. The bot creates separate opencode sessions for each thread in channels where it is added.

```bash
# Edit .env with your Slack app credentials
bun dev
```

--------------------------------

### Start local development preview server

Source: https://github.com/anomalyco/opencode/blob/dev/packages/docs/README.md

Execute at the root of your documentation where docs.json is located to launch the local preview server at http://localhost:3000.

```shell
mint dev
```

--------------------------------

### Port conflict fallback message

Source: https://github.com/anomalyco/opencode/blob/dev/packages/docs/development.mdx

Example message displayed when Mintlify attempts to start on an unavailable port and falls back to the next available port.

```md
Port 3000 is already in use. Trying 3001 instead.
```

--------------------------------

### Wildcard pattern matching on resource in opencode.json

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/policies.mdx

Applies a wildcard pattern using * to match zero or more characters in the resource ID. This example denies any provider starting with company-.

```json
{
  "$schema": "https://opencode.ai/config.json",
  "experimental": {
    "policies": [
      {
        "effect": "deny",
        "action": "provider.use",
        "resource": "company-*"
      }
    ]
  }
}
```

--------------------------------

### POST /project/init

Source: https://github.com/anomalyco/opencode/blob/dev/specs/project.md

Initializes a new project.

```APIDOC
## POST /project/init

### Description
Initializes a new project instance.

### Method
POST

### Endpoint
/project/init

### Response
#### Success Response
- Returns `Project`
```

--------------------------------

### Add a custom LSP server with command and extensions

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/lsp.mdx

Define a new LSP entry by specifying the startup `command` array and the file `extensions` the server should handle.

```json
{
  "$schema": "https://opencode.ai/config.json",
  "lsp": {
    "custom-lsp": {
      "command": ["custom-lsp-server", "--stdio"],
      "extensions": [".custom"]
    }
  }
}
```

--------------------------------

### Sequence catalog initial load and background plugin boot

Source: https://github.com/anomalyco/opencode/blob/dev/specs/v2/catalog-config-plugin-lifecycle.md

Builds a baseline catalog immediately from available plugins to avoid blocking readiness, then loads slower plugins asynchronously in a background fiber.

```ts
LocationServiceMap.get(ref)
  → build location layer
    → Catalog.layer creates empty catalog state
    → PluginBoot.layer activates immediately available plugins
      → ConfigProviderPlugin installs Catalog.transform()
      → ModelsDevPlugin installs Catalog.transform()
      → AuthPlugin installs Catalog.transform()
    → Catalog.layer applies active transforms during boot
    → apply policy
    → materialize baseline provider/model catalog
  → PluginBoot baseline ready
    → Frontend.fetchCatalog()

PluginBoot background fiber
  → install/update plugin packages concurrently
  → activate completed plugins
    → Catalog.transform()
      → transform(updateCatalog)
        → Catalog internally rebuilds
          → Catalog.Event.Updated
            → Frontend.refetchCatalog()
```

--------------------------------

### Install Agents SDK

Source: https://github.com/anomalyco/opencode/blob/dev/packages/opencode/test/fixture/skills/agents-sdk/SKILL.md

Installs the `agents` package using npm if it is not already installed.

```bash
npm install agents
```

--------------------------------

### Initialize Project Configuration with /init

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/index.mdx

Analyzes the codebase and creates an AGENTS.md file in the root directory to guide OpenCode's understanding.

```bash
/init
```

--------------------------------

### Install OpenCode CLI via curl or package managers

Source: https://github.com/anomalyco/opencode/blob/dev/README.md

Installs the OpenCode CLI using the install script or via various package managers across Linux, macOS, and Windows. Versions older than 0.1.x should be removed prior to installation.

```bash
# YOLO
curl -fsSL https://opencode.ai/install | bash

# Package managers
npm i -g opencode-ai@latest        # or bun/pnpm/yarn
scoop install opencode             # Windows
choco install opencode             # Windows
brew install anomalyco/tap/opencode # macOS and Linux (recommended, always up to date)
brew install opencode              # macOS and Linux (official brew formula, updated less)
sudo pacman -S opencode            # Arch Linux (Stable)
paru -S opencode-bin               # Arch Linux (Latest from AUR)
mise use -g opencode               # Any OS
nix run nixpkgs#opencode           # or github:anomalyco/opencode for latest dev branch
```

--------------------------------

### Navigate to Project Directory

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/index.mdx

Change into the target project folder before launching OpenCode.

```bash
cd /path/to/project
```

--------------------------------

### GET /session/:id/diff

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/server.mdx

Get the file diffs associated with a session.

```APIDOC
## GET /session/:id/diff

### Description
Get the diff for this session.

### Method
GET

### Endpoint
/session/:id/diff

### Parameters
#### Path Parameters
- **id** (string) - Required - Session identifier

#### Query Parameters
- **messageID** (string) - Optional - Message identifier to diff against

### Response
#### Success Response (200)
- **Returns** (FileDiff[]) - Array of FileDiff objects
```

--------------------------------

### Create a new SolidStart project

Source: https://github.com/anomalyco/opencode/blob/dev/packages/console/app/README.md

Initialize a new SolidStart project in the current working directory or a specified folder using npm init.

```bash
# create a new project in the current directory
npm init solid@latest

# create a new project in my-app
npm init solid@latest my-app
```

--------------------------------

### Create opencode client with createOpencode

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/sdk.mdx

Initializes and starts both an opencode server instance and a client.

```javascript
import { createOpencode } from "@opencode-ai/sdk"

const { client } = await createOpencode()
```

--------------------------------

### Configure and start createOpencode instance with custom options

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/da/sdk.mdx

Tilpasser vertsnavn, port og konfigurasjonsmodell ved opprettelse av en OpenCode-forekomst, og lukker serveren etter bruk.

```javascript
import { createOpencode } from "@opencode-ai/sdk"

const opencode = await createOpencode({
  hostname: "127.0.0.1",
  port: 4096,
  config: {
    model: "anthropic/claude-3-5-sonnet-20241022",
  },
})

console.log(`Server running at ${opencode.server.url}`)

opencode.server.close()
```

--------------------------------

### Create an agent with opencode agent create

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/agents.mdx

Runs an interactive prompt to configure agent scope, description, system prompt, and permissions, saving the result to a markdown file.

```bash
opencode agent create
```

--------------------------------

### Install a plugin with opencode plugin

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/cli.mdx

Install a plugin module and update your OpenCode configuration.

```bash
opencode plugin <module>
```

--------------------------------

### GET /project/:projectID/session/:sessionID/file

Source: https://github.com/anomalyco/opencode/blob/dev/specs/project.md

Gets the content or patch of a file within a session.

```APIDOC
## GET /project/:projectID/session/:sessionID/file

### Description
Retrieves raw or patch file content for a session.

### Method
GET

### Endpoint
/project/:projectID/session/:sessionID/file

### Parameters
#### Path Parameters
- **projectID** (string) - Required - Identifier of the project
- **sessionID** (string) - Required - Identifier of the session

### Response
#### Success Response
- Returns `{ type: "raw" | "patch", content: string }`
```

--------------------------------

### Install dependencies

Source: https://github.com/anomalyco/opencode/blob/dev/packages/app/README.md

Installs project dependencies using npm, pnpm, or yarn after cloning the template.

```bash
$ npm install # or pnpm install or yarn install
```

--------------------------------

### Install OpenCode on Windows using Chocolatey

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/index.mdx

Install OpenCode on Windows using the Chocolatey package manager.

```bash
choco install opencode
```

--------------------------------

### Verify Agents SDK Installation

Source: https://github.com/anomalyco/opencode/blob/dev/packages/opencode/test/fixture/skills/agents-sdk/SKILL.md

Run this command to check if the `agents` package is already installed in your project.

```bash
npm ls agents  # Should show agents package
```

--------------------------------

### Install wl-clipboard for Wayland on Linux

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/troubleshooting.mdx

Installs wl-clipboard on Linux systems running Wayland to restore copy/paste functionality in OpenCode.

```bash
apt install -y wl-clipboard
```

--------------------------------

### Prompt to invoke mcp_everything tool

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/mcp-servers.mdx

Example prompt syntax for instructing OpenCode to use the mcp_everything tool.

```txt
use the mcp_everything tool to add the number 3 and 4
```

--------------------------------

### Create a skill definition in SKILL.md

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/skills.mdx

Example SKILL.md file placed in .opencode/skills/git-release/SKILL.md containing YAML frontmatter and instructional markdown.

```markdown
---
name: git-release
description: Create consistent releases and changelogs
license: MIT
compatibility: opencode
metadata:
  audience: maintainers
  workflow: github
---

## What I do

- Draft release notes from merged PRs
- Propose a version bump
- Provide a copy-pasteable `gh release create` command

## When to use me

Use this when you are preparing a tagged release.
Ask clarifying questions if the target versioning scheme is unclear.
```

--------------------------------

### Set custom installation directory for OpenCode

Source: https://github.com/anomalyco/opencode/blob/dev/README.md

Specifies a custom target path such as OPENCODE_INSTALL_DIR or XDG_BIN_DIR when executing the install script.

```bash
# Examples
OPENCODE_INSTALL_DIR=/usr/local/bin curl -fsSL https://opencode.ai/install | bash
XDG_BIN_DIR=$HOME/.local/bin curl -fsSL https://opencode.ai/install | bash
```

--------------------------------

### Build and package desktop app

Source: https://github.com/anomalyco/opencode/blob/dev/CONTRIBUTING.md

Creates a production build and packages the desktop application.

```bash
bun run --cwd packages/desktop build
bun run --cwd packages/desktop package
```

--------------------------------

### Start OpenCode with --auto

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/permissions.mdx

Starts OpenCode in auto mode to automatically approve permission requests that are not explicitly denied.

```bash
opencode --auto
```

--------------------------------

### Start OpenCode TUI in current directory

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/es/tui.mdx

Starts the interactive terminal user interface (TUI) for the current working directory.

```bash
opencode
```

--------------------------------

### Start OpenCode TUI default

Source: https://github.com/anomalyco/opencode/blob/dev/packages/web/src/content/docs/cli.mdx

Runs OpenCode without arguments, which starts the terminal user interface (TUI) by default.

```bash
opencode
```

--------------------------------

### Start headless API server on default port

Source: https://github.com/anomalyco/opencode/blob/dev/CONTRIBUTING.md

Starts the OpenCode headless API server on default port 4096.

```bash
bun dev serve
```