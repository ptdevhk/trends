### Quickstart with Peekaboo CLI

Source: https://github.com/openclaw/openclaw/blob/main/skills/peekaboo/SKILL.md

Basic commands to get started with Peekaboo, including checking permissions, listing applications, taking a screenshot, clicking an element, and typing text.

```bash
peekaboo permissions
peekaboo list apps --json
peekaboo see --annotate --path /tmp/peekaboo-see.png
peekaboo click --on B1
peekaboo type "Hello" --return
```

--------------------------------

### Install and start Tailscale on VPS

Source: https://github.com/openclaw/openclaw/blob/main/docs/help/faq.md

Standard installation and activation commands for Tailscale on a Linux VPS.

```bash
curl -fsSL https://tailscale.com/install.sh | sh
sudo tailscale up
```

--------------------------------

### Run guided setup

Source: https://github.com/openclaw/openclaw/blob/main/docs/channels/clickclack.md

Launch the interactive onboarding wizard to configure the ClickClack channel.

```bash
openclaw onboard
```

--------------------------------

### Install and Run Node Host

Source: https://github.com/openclaw/openclaw/blob/main/docs/plugins/google-meet.md

Install the Google Meet plugin and start the node host within the VM environment.

```bash
openclaw plugins install npm:@openclaw/google-meet
openclaw node run --host <gateway-host> --port 18789 --display-name parallels-macos
```

--------------------------------

### Launch interactive channel setup

Source: https://github.com/openclaw/openclaw/blob/main/docs/cli/channels.md

Open the guided interactive setup wizard for a specific channel using positional arguments or the --channel flag.

```bash
openclaw channels add telegram
openclaw channels add --channel telegram
```

--------------------------------

### Force Installation Method

Source: https://github.com/openclaw/openclaw/blob/main/docs/help/faq-first-run.md

Use the installer script to force a specific installation method during initial setup.

```bash
curl -fsSL --proto '=https' --tlsv1.2 https://openclaw.ai/install.sh | bash -s -- --install-method git
```

```bash
curl -fsSL --proto '=https' --tlsv1.2 https://openclaw.ai/install.sh | bash -s -- --install-method npm
```

--------------------------------

### Install and inspect an external plugin

Source: https://github.com/openclaw/openclaw/blob/main/docs/plugins/plugin-inventory.md

Example workflow for installing an official external plugin, restarting the gateway, and inspecting the runtime status.

```bash
openclaw plugins install @openclaw/discord
openclaw gateway restart
openclaw plugins inspect discord --runtime --json
```

--------------------------------

### Install OpenClaw and run onboarding

Source: https://github.com/openclaw/openclaw/blob/main/docs/install/macos-vm.md

Installs the latest version of OpenClaw globally and initiates the daemon installation and provider setup.

```bash
npm install -g openclaw@latest
openclaw onboard --install-daemon
```

--------------------------------

### Initial gateway setup flow

Source: https://github.com/openclaw/openclaw/blob/main/docs/install/clawdock.md

Starts the gateway container, synchronizes the token, and launches the dashboard.

```bash
clawdock-start
clawdock-fix-token
clawdock-dashboard
```

--------------------------------

### Run the Feishu channel setup wizard

Source: https://github.com/openclaw/openclaw/blob/main/docs/channels/feishu.md

Initiates the interactive setup process for the Feishu channel, including plugin installation and credential configuration.

```bash
openclaw channels login --channel feishu
```

--------------------------------

### Run OpenClaw setup commands

Source: https://github.com/openclaw/openclaw/blob/main/docs/web/tui.md

Commands for starting the setup assistant interactively or executing specific configuration tasks.

```bash
openclaw setup                       # start interactively
openclaw setup -m "status"           # run one request and exit
openclaw setup -m "set default model openai/gpt-5.2" --yes   # apply a config write
```

--------------------------------

### Install and initialize Honcho plugin via CLI

Source: https://github.com/openclaw/openclaw/blob/main/docs/concepts/memory-honcho.md

Run these commands to install the package and perform initial setup. The setup process includes credential configuration and optional migration of existing memory files.

```bash
openclaw plugins install @honcho-ai/openclaw-honcho
openclaw honcho setup
openclaw gateway --force
```

--------------------------------

### Start LiteLLM Proxy and Run OpenClaw Manually

Source: https://github.com/openclaw/openclaw/blob/main/docs/providers/litellm.md

Install and start the LiteLLM proxy locally, then export the API key and launch OpenClaw.

```bash
pip install 'litellm[proxy]'
litellm --model claude-opus-4-6
```

```bash
export LITELLM_API_KEY="your-litellm-key"
openclaw
```

--------------------------------

### Perform an airgapped installation

Source: https://github.com/openclaw/openclaw/blob/main/docs/install/docker.md

Load a pre-transferred image and run the setup script in offline mode.

```bash
docker load -i openclaw-image.tar
export OPENCLAW_IMAGE="ghcr.io/openclaw/openclaw:latest"
./scripts/docker/setup.sh --offline
```

--------------------------------

### Initialize and run the development environment

Source: https://github.com/openclaw/openclaw/blob/main/qa/convex-credential-broker/README.md

Commands to navigate to the project directory, install dependencies, and start the Convex development server.

```bash
cd qa/convex-credential-broker
npm install
npx convex dev
```

--------------------------------

### Run Control UI in Development Mode

Source: https://github.com/openclaw/openclaw/blob/main/docs/web/control-ui.md

Starts the development server for the Control UI.

```bash
pnpm ui:dev
```

--------------------------------

### GET /__openclaw__/assistant-media

Source: https://github.com/openclaw/openclaw/blob/main/docs/web/control-ui.md

Retrieves metadata for assistant local-media previews, returning a short-lived media ticket for secure media rendering.

```APIDOC
## GET /__openclaw__/assistant-media

### Description
Checks availability of assistant local-media and returns a short-lived mediaTicket for the requested source path.

### Method
GET

### Endpoint
/__openclaw__/assistant-media

### Parameters
#### Query Parameters
- **meta** (integer) - Required - Must be set to 1.
- **source** (string) - Required - The path to the media source.
```

--------------------------------

### Run setup commands

Source: https://github.com/openclaw/openclaw/blob/main/docs/cli/openclaw.md

Use these commands to configure the workspace and Gateway state after initial onboarding.

```text
setup
setup workspace ~/Projects/work
```

--------------------------------

### Run onboarding in the container

Source: https://github.com/openclaw/openclaw/blob/main/docs/install/podman.md

Execute the setup command within the container to initialize the gateway.

```bash
./scripts/run-openclaw-podman.sh launch setup
```

--------------------------------

### Install WeChat Plugin

Source: https://github.com/openclaw/openclaw/blob/main/docs/channels/wechat.md

Provides options for quick installation, manual setup with configuration, or installing the legacy version for compatibility.

```bash
npx -y @tencent-weixin/openclaw-weixin-cli install
```

```bash
openclaw plugins install "@tencent-weixin/openclaw-weixin"
openclaw config set plugins.entries.openclaw-weixin.enabled true
```

```bash
openclaw plugins install @tencent-weixin/openclaw-weixin@legacy
```

--------------------------------

### GET /avatar/<agentId>

Source: https://github.com/openclaw/openclaw/blob/main/docs/web/control-ui.md

Retrieves an avatar image or metadata for a specific agent. Requires a gateway token as a bearer header when gateway authentication is enabled.

```APIDOC
## GET /avatar/<agentId>

### Description
Retrieves the avatar image for the specified agent. If the meta query parameter is set to 1, it returns the avatar metadata instead.

### Method
GET

### Endpoint
/avatar/<agentId>

### Parameters
#### Path Parameters
- **agentId** (string) - Required - The unique identifier of the agent.

#### Query Parameters
- **meta** (integer) - Optional - Set to 1 to retrieve avatar metadata instead of the image.
```

--------------------------------

### Quick Start Commands for sag

Source: https://github.com/openclaw/openclaw/blob/main/skills/sag/SKILL.md

Basic commands to get started with `sag` for text-to-speech, including simple synthesis, specifying a voice, listing available voices, and getting model-specific tips.

```bash
sag "Hello there"
```

```bash
sag speak -v "Roger" "Hello"
```

```bash
sag voices
```

```bash
sag prompting
```

--------------------------------

### Quick Path VM Setup

Source: https://github.com/openclaw/openclaw/blob/main/docs/install/macos-vm.md

Use these commands for a fast-track setup if you are already familiar with Lume. It handles VM creation and headless execution in two steps.

```bash
lume create openclaw --os macos --ipsw latest
```

```bash
lume run openclaw --no-display
```

--------------------------------

### Run Classic Onboarding Wizard

Source: https://github.com/openclaw/openclaw/blob/main/docs/start/wizard.md

Launches the classic wizard for custom providers, remote Gateway setup, and advanced controls.

```bash
openclaw onboard --classic
```

--------------------------------

### Stop and start Gateway service

Source: https://github.com/openclaw/openclaw/blob/main/docs/help/faq.md

Explicitly stop and start the installed gateway service.

```bash
openclaw gateway stop
openclaw gateway start
```

--------------------------------

### Retrieve Teams installation link

Source: https://github.com/openclaw/openclaw/blob/main/docs/channels/msteams.md

Get the direct installation link for a registered Teams application.

```bash
teams app get <teamsAppId> --install-link
```

--------------------------------

### Setup OpenClaw with Podman

Source: https://github.com/openclaw/openclaw/blob/main/docs/install/podman.md

Run the setup script to build the image and configure the environment. Use the --quadlet flag for systemd-managed services.

```bash
./scripts/podman/setup.sh --quadlet
```

--------------------------------

### Start Speech Swift Server

Source: https://github.com/openclaw/openclaw/blob/main/docs/tools/tts.md

Install the speech package and start the local server on port 8080.

```bash
brew install speech
speech-server --port 8080
```

--------------------------------

### TTS configuration examples

Source: https://github.com/openclaw/openclaw/blob/main/docs/plugins/voice-call.md

Examples demonstrating core TTS setup, plugin-specific overrides, and model configuration.

```json5
{
  tts: {
    provider: "openai",
    providers: {
      openai: { speakerVoice: "alloy" },
    },
  },
}
```

```json5
{
  plugins: {
    entries: {
      "voice-call": {
        config: {
          tts: {
            provider: "elevenlabs",
            providers: {
              elevenlabs: {
                apiKey: "elevenlabs_key",
                speakerVoiceId: "pMsXgVXv3BLzUgSXRplE",
                modelId: "eleven_multilingual_v2",
              },
            },
          },
        },
      },
    },
  },
}
```

```json5
{
  plugins: {
    entries: {
      "voice-call": {
        config: {
          tts: {
            providers: {
              openai: {
                model: "gpt-4o-mini-tts",
                speakerVoice: "marin",
              },
            },
          },
        },
      },
    },
  },
}
```

--------------------------------

### Run configuration wizards

Source: https://github.com/openclaw/openclaw/blob/main/docs/gateway/configuration.md

Use the onboard or configure commands to launch interactive setup flows.

```bash
openclaw onboard       # full onboarding flow
openclaw configure     # config wizard
```

--------------------------------

### Run OpenClaw onboarding

Source: https://github.com/openclaw/openclaw/blob/main/docs/start/getting-started.md

Initiates the setup wizard to configure model providers, API keys, and the gateway daemon.

```bash
openclaw onboard --install-daemon
```

--------------------------------

### Start dev Gateway with hot reload

Source: https://github.com/openclaw/openclaw/blob/main/docs/start/setup.md

Install dependencies and start the TypeScript Gateway watcher for active development.

```bash
pnpm install
# First run only (or after resetting local OpenClaw config/workspace)
pnpm openclaw setup
pnpm gateway:watch
```

--------------------------------

### Reload Existing OpenClaw Docker Install

Source: https://github.com/openclaw/openclaw/blob/main/docs/install/docker.md

Commands to reload environment variables and run setup for an existing installation.

```bash
set -a
. ./.env
set +a
export OPENCLAW_HOME_VOLUME="${OPENCLAW_HOME_VOLUME:-openclaw_home}"
./scripts/docker/setup.sh
```

--------------------------------

### Initialize New OpenClaw Docker Install

Source: https://github.com/openclaw/openclaw/blob/main/docs/install/docker.md

Environment setup for a fresh installation using a persistent home volume.

```bash
export OPENCLAW_IMAGE="ghcr.io/openclaw/openclaw:latest"
export OPENCLAW_HOME_VOLUME="openclaw_home"
./scripts/docker/setup.sh
```

--------------------------------

### Build and run the application

Source: https://github.com/openclaw/openclaw/blob/main/apps/linux/README.md

Commands to install dependencies and launch the development environment.

```bash
pnpm install
cd apps/linux/src-tauri
cargo run
cargo build
```

--------------------------------

### Install OpenClaw CLI manually

Source: https://github.com/openclaw/openclaw/blob/main/docs/platforms/mac/bundled-gateway.md

Use this command to install the OpenClaw CLI globally when automatic setup fails.

```bash
npm install -g openclaw@<version>
```

--------------------------------

### Setup and join meeting

Source: https://github.com/openclaw/openclaw/blob/main/docs/plugins/google-meet.md

Run the setup command and join a specific Google Meet URL.

```bash
openclaw googlemeet setup
openclaw googlemeet join https://meet.google.com/abc-defg-hij
```

--------------------------------

### Start LM Studio Server

Source: https://github.com/openclaw/openclaw/blob/main/docs/providers/lmstudio.md

Starts the LM Studio server on a specified port. Use this for desktop or GUI-based setups.

```bash
lms server start --port 1234
```

--------------------------------

### Initialize Teams CLI

Source: https://github.com/openclaw/openclaw/blob/main/docs/channels/msteams.md

Install the preview Teams CLI and authenticate with your tenant.

```bash
npm install -g @microsoft/teams.cli@preview
teams login
teams status   # verify you're logged in and see your tenant info
```

--------------------------------

### Run Classic Onboarding Wizard

Source: https://github.com/openclaw/openclaw/blob/main/docs/start/onboarding-overview.md

Launches the classic wizard for detailed configuration of models, channels, skills, and gateway settings.

```bash
openclaw onboard --classic
```

--------------------------------

### Configure Pinned npm Installation

Source: https://github.com/openclaw/openclaw/blob/main/docs/plugins/sdk-setup.md

Example of specifying a pinned npm package version and integrity hash for plugin installation.

```json
{
  "openclaw": {
    "install": {
      "npmSpec": "@wecom/wecom-openclaw-plugin@1.2.3",
      "expectedIntegrity": "sha512-REPLACE_WITH_NPM_DIST_INTEGRITY",
      "defaultChoice": "npm"
    }
  }
}
```

--------------------------------

### Inspect Local Setup Logs

Source: https://github.com/openclaw/openclaw/blob/main/docs/platforms/windows.md

Opens the OpenClaw Companion setup log file in Notepad to diagnose installation or configuration failures.

```powershell
notepad "$env:LOCALAPPDATA\OpenClawTray\Logs\Setup\easy-setup-latest.txt"
```

--------------------------------

### Common authentication workflow examples

Source: https://github.com/openclaw/openclaw/blob/main/docs/cli/models.md

Practical examples for logging into providers, managing API keys, and cleaning up authentication profiles.

```bash
openclaw models auth login --provider openai --set-default
openclaw models auth login --provider openai --method api-key
openclaw models auth paste-api-key --provider openai
openclaw models auth list --provider openai
openclaw models auth logout openai:manual --yes
```

--------------------------------

### Install Go catalog plugin

Source: https://github.com/openclaw/openclaw/blob/main/docs/providers/opencode.md

Install the required plugin and restart the gateway to enable the Go catalog.

```bash
openclaw plugins install @openclaw/opencode-go-provider
openclaw gateway restart
```

--------------------------------

### Install OpenClaw via Shell Script

Source: https://github.com/openclaw/openclaw/blob/main/docs/install/azure.md

Downloads and runs the official OpenClaw installation script which handles Node.js and dependency setup.

```bash
curl -fsSL https://openclaw.ai/install.sh -o /tmp/install.sh
bash /tmp/install.sh
rm -f /tmp/install.sh
```

--------------------------------

### Onboard OpenCode Go interactively

Source: https://github.com/openclaw/openclaw/blob/main/docs/providers/opencode-go.md

Run the onboarding process with the opencode-go authentication choice.

```bash
openclaw onboard --auth-choice opencode-go
```

--------------------------------

### Example stdin for install policy

Source: https://github.com/openclaw/openclaw/blob/main/docs/tools/skills-config.md

The JSON object provided to the policy command via stdin containing metadata about the installation request.

```json
{
  "protocolVersion": 1,
  "openclawVersion": "2026.6.1",
  "targetType": "skill",
  "targetName": "weather",
  "sourcePath": "/var/folders/.../openclaw-skill-clawhub/root",
  "sourcePathKind": "directory",
  "source": {
    "kind": "clawhub",
    "authority": "openclaw",
    "mutable": false,
    "network": true
  },
  "origin": {
    "type": "clawhub",
    "registry": "https://clawhub.openclaw.ai",
    "slug": "weather",
    "version": "1.0.0"
  },
  "request": {
    "kind": "skill-install",
    "mode": "install",
    "requestedSpecifier": "clawhub:weather@1.0.0"
  },
  "skill": {
    "installId": "clawhub"
  }
}
```

--------------------------------

### Non-interactive setup

Source: https://github.com/openclaw/openclaw/blob/main/docs/providers/deepseek.md

Perform a headless or scripted onboarding setup by passing all configuration flags directly.

```bash
openclaw onboard --non-interactive \
  --mode local \
  --auth-choice deepseek-api-key \
  --deepseek-api-key "$DEEPSEEK_API_KEY" \
  --skip-health \
  --accept-risk
```

--------------------------------

### Initialize Development Environment

Source: https://github.com/openclaw/openclaw/blob/main/docs/gateway/index.md

Commands to set up the development environment, launch the gateway, and check status.

```bash
openclaw --dev setup
openclaw --dev gateway --allow-unconfigured
openclaw --dev status
```

--------------------------------

### Setup and join meeting via CLI

Source: https://github.com/openclaw/openclaw/blob/main/docs/plugins/zoom-meetings.md

Commands to initialize the setup and join a specific Zoom meeting URL.

```bash
openclaw zoommeetings setup
openclaw zoommeetings join 'https://zoom.us/j/1234567890'
```

--------------------------------

### Configure Matrix interactively

Source: https://github.com/openclaw/openclaw/blob/main/docs/channels/matrix.md

Run the interactive setup wizard to configure homeserver, authentication, and room access.

```bash
openclaw channels add
openclaw configure --section channels
```

--------------------------------

### mcporter Quick Start Commands

Source: https://github.com/openclaw/openclaw/blob/main/skills/mcporter/SKILL.md

Basic commands to quickly get started with `mcporter` for listing servers and making simple tool calls.

```bash
mcporter list
```

```bash
mcporter list <server> --schema
```

```bash
mcporter call <server.tool> key=value
```

--------------------------------

### Example Project Environment File

Source: https://github.com/openclaw/openclaw/blob/main/scripts/clawdock/README.md

Shows a typical `<project>/.env` file, configuring Docker infrastructure settings like image name, ports, and gateway token.

```bash
OPENCLAW_CONFIG_DIR=/Users/you/.openclaw
OPENCLAW_WORKSPACE_DIR=/Users/you/.openclaw/workspace
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790
OPENCLAW_GATEWAY_BIND=lan
OPENCLAW_GATEWAY_TOKEN=<generated-by-docker-setup>
OPENCLAW_AUTH_PROFILE_SECRET_DIR=/Users/you/.openclaw-auth-profile-secrets
OPENCLAW_IMAGE=openclaw:local
```

--------------------------------

### Run interactive onboarding

Source: https://github.com/openclaw/openclaw/blob/main/docs/providers/longcat.md

Start the interactive onboarding process to configure the LongCat API key.

```bash
openclaw onboard --auth-choice longcat-api-key
```

--------------------------------

### Automated OpenClaw Installation via Shelley

Source: https://github.com/openclaw/openclaw/blob/main/docs/install/exe-dev.md

Use this prompt with the Shelley agent to automate the installation, Nginx configuration, and initial setup of OpenClaw on an exe.dev VM.

```text
Set up OpenClaw (https://docs.openclaw.ai/install) on this VM. Use the non-interactive and accept-risk flags for openclaw onboarding. Add the supplied auth or token as needed. Configure nginx to forward from the default port 18789 to the root location on the default enabled site config, making sure to enable Websocket support. Pairing is done by "openclaw devices list" and "openclaw devices approve <request id>". Make sure the dashboard shows that OpenClaw's health is OK. exe.dev handles forwarding from port 8000 to port 80/443 and HTTPS for us, so the final "reachable" should be <vm-name>.exe.xyz, without port specification.
```

--------------------------------

### Run Onboarding for Standard Plan

Source: https://github.com/openclaw/openclaw/blob/main/docs/providers/qwen.md

Execute the onboarding command to authenticate with the Standard pay-as-you-go endpoint.

```bash
openclaw onboard --auth-choice qwen-standard-api-key
```

```bash
openclaw onboard --auth-choice qwen-standard-api-key-cn
```

--------------------------------

### AI-Evaluated Discretion Condition Example

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/compiler.md

This example demonstrates using discretion markers (`**...**`) for AI-evaluated conditions to guide session execution based on code analysis.

```prose
let analysis = session "Analyze the codebase"

if **the code has security vulnerabilities**:
  session "Fix security issues immediately"
    context: analysis
elif **the code has performance issues**:
  session "Optimize performance bottlenecks"
    context: analysis
else:
  session "Proceed with normal review"
    context: analysis
```

--------------------------------

### Configure Skill Installer Specifications

Source: https://github.com/openclaw/openclaw/blob/main/docs/tools/skills.md

Define installation methods like Homebrew within the metadata block to enable automated dependency setup in the macOS Skills UI.

```markdown
---
name: gemini
description: Use Gemini CLI for coding assistance and Google search lookups.
metadata:
  {
    "openclaw":
      {
        "emoji": "♊️",
        "requires": { "bins": ["gemini"] },
        "install":
          [
            {
              "id": "brew",
              "kind": "brew",
              "formula": "gemini-cli",
              "bins": ["gemini"],
              "label": "Install Gemini CLI (brew)",
            },
          ],
      },
  }
---
```

--------------------------------

### Detailed examples for adding todos

Source: https://github.com/openclaw/openclaw/blob/main/skills/things-mac/SKILL.md

Examples demonstrating how to add todos with specific details like notes, project/area, heading, tags, checklist items, and multi-line input from STDIN.

```bash
things add "Buy milk"
```

```bash
things add "Buy milk" --notes "2% + bananas"
```

```bash
things add "Book flights" --list "Travel"
```

```bash
things add "Pack charger" --list "Travel" --heading "Before"
```

```bash
things add "Call dentist" --tags "health,phone"
```

```bash
things add "Trip prep" --checklist-item "Passport" --checklist-item "Tickets"
```

```bash
cat <<'EOF' | things add -
Title line
Notes line 1
Notes line 2
EOF
```

--------------------------------

### Run onboarding for Xiaomi

Source: https://github.com/openclaw/openclaw/blob/main/docs/providers/xiaomi.md

Execute the onboarding process using either pay-as-you-go or Token Plan authentication methods.

```bash
openclaw onboard --auth-choice xiaomi-api-key
```

```bash
openclaw onboard --auth-choice xiaomi-token-plan-sgp
```

```bash
openclaw onboard --auth-choice xiaomi-api-key --xiaomi-api-key "$XIAOMI_API_KEY"
openclaw onboard --auth-choice xiaomi-token-plan-sgp --xiaomi-token-plan-api-key "$XIAOMI_TOKEN_PLAN_API_KEY"
```

--------------------------------

### Install Docker on Debian VM

Source: https://github.com/openclaw/openclaw/blob/main/docs/install/gcp.md

Install Docker and add the current user to the docker group. This setup allows running containers without requiring sudo for every command.

```bash
sudo apt-get update
sudo apt-get install -y git curl ca-certificates
curl -fsSL https://get.docker.com | sudo sh
sudo usermod -aG docker $USER
```

--------------------------------

### Defining a setup entry point

Source: https://github.com/openclaw/openclaw/blob/main/docs/plugins/sdk-setup.md

Use setup-entry.ts to define a lightweight entry point for setup-related tasks, avoiding heavy runtime dependencies.

```typescript
// setup-entry.ts
import { defineSetupPluginEntry } from "openclaw/plugin-sdk/channel-core";
import { myChannelPlugin } from "./src/channel.js";

export default defineSetupPluginEntry(myChannelPlugin);
```