display_name: Playwright CLI
short_description: Automate browser workflows via playwright-cli with the project chrome-debug/CDP defaults.
default_prompt: Use the playwright-cli workflow. Prefer attaching to the local chrome-debug CDP endpoint, validate key UI state with deterministic selector checks, and keep browser automation commands runnable from the repository root.
